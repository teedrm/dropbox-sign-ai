{
    "version": 2,
    "builds": [
        {
            "src": "server/main-page.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "server/main-page.py/$1"
        }
    ]
}
